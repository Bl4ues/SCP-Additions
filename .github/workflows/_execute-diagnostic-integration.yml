name: Execute Diagnostic Integration (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary diagnostic integration workflow retired after the Diagnostic Table merge."
