name: Build canonical SCP Inventory pass 2

on:
  push:
    branches:
      - integration/scp-additions-3.0

permissions:
  contents: write

jobs:
  build:
    if: contains(github.event.head_commit.message, 'run canonical build pass two')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'
      - uses: gradle/actions/setup-gradle@v4
      - name: Build
        run: |
          chmod +x gradlew
          set +e
          ./gradlew clean build --stacktrace > migration-reference/scpinventory-final/CANONICAL_BUILD_2.log 2>&1
          printf '%s\n' "$?" > migration-reference/scpinventory-final/CANONICAL_BUILD_2_STATUS.txt
          exit 0
      - name: Commit diagnostics
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add migration-reference/scpinventory-final/CANONICAL_BUILD_2.log migration-reference/scpinventory-final/CANONICAL_BUILD_2_STATUS.txt
          git commit -m "chore(inventory): record canonical build pass two"
          git push origin HEAD:integration/scp-additions-3.0
