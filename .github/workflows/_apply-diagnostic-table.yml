name: Archivist Table Integration (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary Archivist Table and Document Holder integration workflow retired."
