name: Apply Configuration Center Repairs

on:
  push:
    branches:
      - master
    paths:
      - .github/workflows/apply_config_center_repairs.yml

permissions:
  contents: write

jobs:
  repair:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Smoke test
        run: echo "repair workflow ready"
