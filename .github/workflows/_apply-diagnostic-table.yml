name: Archivist Table Double-Sided Face Repair (retired)

on:
  workflow_dispatch:

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary Archivist's Table double-sided face repair workflow retired."
