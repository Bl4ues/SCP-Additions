name: Integrate SCP Sign Reference Alignment

on:
  pull_request:
    branches: [master]
    types: [opened, synchronize, reopened]

permissions:
  contents: write

jobs:
  integrate:
    if: github.head_ref == 'fix/scp-sign-reference-alignment'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout fix branch
        uses: actions/checkout@v4
        with:
          ref: fix/scp-sign-reference-alignment
          fetch-depth: 0

      - name: Configure Git
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

      - name: Apply targeted alignment changes
        run: |
          python3 scripts/apply_scp_sign_reference_alignment.py
          rm scripts/apply_scp_sign_reference_alignment.py

      - name: Build
        run: |
          chmod +x gradlew
          ./gradlew build --no-daemon

      - name: Commit final branch state
        run: |
          git add -A
          git commit -m "Align SCP sign text with Unity reference [skip ci]"
          git push origin HEAD:fix/scp-sign-reference-alignment
