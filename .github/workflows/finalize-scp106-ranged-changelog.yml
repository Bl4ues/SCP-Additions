name: Temporary SCP-106 Changelog Workflow Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo "This temporary workflow is disabled and can be removed."
